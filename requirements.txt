# SQLBuster - No required external dependencies
# This project uses only Python standard library

# Development dependencies (install with: pip install -r requirements.txt)
pytest>=7.0.0
pytest-cov>=4.0.0
mypy>=1.0.0
black>=23.0.0
isort>=5.12.0
